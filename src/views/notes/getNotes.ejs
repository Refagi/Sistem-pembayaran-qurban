<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css" />
  <style>
    /* CSS untuk tabel */
    table {
      width: 100%; /* Lebar tabel penuh */
      border-collapse: collapse; /* Menghilangkan jarak border */
      margin-top: 10px; /* Jarak antara tabel dan elemen sebelumnya */
    }
    th, td {
      padding: 8px; /* Padding untuk sel */
      border-bottom: 1px solid #ddd; /* Border bawah pada sel */
      text-align: left; /* Teks rata kiri */
    }

  </style>
</head>
<body>
<!-- Modal -->
<!-- error.ejs -->
<div class="modal fade show" id="modalSignin" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true" style="display: block;">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5 fw-bolder" id="staticBackdropLabel">Catatan <%= notes.name %></h1>
      </div>
      <div class="modal-body">
        <table>
          <tbody>
            <tr>
              <th>Id </th>
              <td><%= notes.id %></td>
            </tr>

            <tr>
              <th>Id User </th>
              <td><%= notes.user_id %></td>
            </tr>

            <tr>
              <th>Id Hewan </th>
              <td><%= notes.animal_id %></td>
            </tr>

            <tr>
              <th>Jumlah Pembayaran </th>
              <td><%= notes.payment_amount %></td>
            </tr>

            <tr>
              <th>Status </th>
              <td><%= notes.paidOff %></td>
            </tr>

            <tr>
              <th>Nama User </th>
              <td><%= notes.name %></td>
            </tr>

            <tr>
              <th>Email User </th>
              <td><%= notes.email %></td>
            </tr>

            <tr>
              <th>Pembayaran </th>
              <td><%= notes.payments.length %> Transaksi</td>
            </tr>

          </tbody>
        </table>
      </div>
      <div class="modal-footer">
        <a type="button" class="btn btn-outline-primary" href="/api/notes">Kembali</a>
      </div>
    </div>
  </div>
</div>
<div class="modal-backdrop fade show"></div>

</body>
<script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
</html>
