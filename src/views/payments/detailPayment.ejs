<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css" />
  <style>
    /* CSS untuk tabel */
    table {
      width: 100%; /* Lebar tabel penuh */
      border-collapse: collapse; /* Menghilangkan jarak border */
      margin-top: 10px; /* Jarak antara tabel dan elemen sebelumnya */
    }
    th, td {
      padding: 8px; /* Padding untuk sel */
      border-bottom: 1px solid #ddd; /* Border bawah pada sel */
      text-align: left; /* Teks rata kiri */
    }

  </style>
</head>
<body>
<div class="modal fade show" id="modalSignin" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true" style="display: block;">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5 fw-bolder" id="staticBackdropLabel">Payment</h1>
      </div>
      <div class="modal-body">
        <div class="table-responsive">
          <table class="table table-striped">
            <% data.forEach(payment => { %>
            <thead>
              <tr class="table-primary">
                <th>Jumlah</th>
                <th>Status Hewan</th>
                <th>Nama</th>
                <th>No handphone</th>
                <th>Informasi</th>
                <th>Tanggal dibuat</th>
                <!-- <th>Notes</th> -->
                <th>Tanggal diperbarui</th>
              </tr>
            </thead>
            <tbody>
                  <tr>
                    <td><%= payment.amount %></td>
                    <td><%= payment.status_animal %></td>
                    <td><%= payment.name %></td>
                    <td><%= payment.handphone %></td>
                    <td><%= payment.information %></td>
                    <td><%= new Date(payment.createdAt).toLocaleString('id-ID', { dateStyle: 'short', timeStyle: 'short' }) %></td>
                    <td><%= new Date(payment.updatedAt).toLocaleString('id-ID', { dateStyle: 'short', timeStyle: 'short' }) %></td>
                  </tr>
            </tbody>
            <% }) %>
          </table>
        </div>
        <div class="modal-footer">
          <a type="button" class="btn btn-outline-primary" href="/api/notes/my-notes/<%= paymentId %>">Go back</a>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="modal-backdrop fade show"></div>

</body>
<script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
</html>
