<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css" />
  <style>
    /* CSS untuk tabel */
    table {
      width: 100%; /* Lebar tabel penuh */
      border-collapse: collapse; /* Menghilangkan jarak border */
      margin-top: 10px; /* Jarak antara tabel dan elemen sebelumnya */
    }
    th, td {
      padding: 8px; /* Padding untuk sel */
      border-bottom: 1px solid #ddd; /* Border bawah pada sel */
      text-align: left; /* Teks rata kiri */
    }

  </style>
</head>
<body>
<!-- Modal -->
<!-- error.ejs -->
<div class="modal fade show" id="modalSignin" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true" style="display: block;">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5 fw-bolder" id="staticBackdropLabel">Pembayaran</h1>
      </div>
      <div class="modal-body">
        <table>
          <tbody>
            <tr>
              <th>Id </th>
              <td><%= payment.id %></td>
            </tr>

            <tr>
              <th>Id Hewan </th>
              <td><%= payment.animal_id %></td>
            </tr>

            <tr>
              <th>Id Catatan </th>
              <td><%= payment.notes_id %></td>
            </tr>

            <tr>
              <th>Jumlah dibayarkan </th>
              <td><%= payment.amount %></td>
            </tr>

            <tr>
              <th>Status hewan </th>
              <td><%= payment.status_animal %></td>
            </tr>

            <tr>
              <th>Nama user </th>
              <td><%= payment.name %></td>
            </tr>

            <tr>
              <th>No handphone </th>
              <td><%= payment.handphone %></td>
            </tr>

            <tr>
              <th>Informasi </th>
              <td><%= payment.information %></td>
            </tr>

          </tbody>
        </table>
      </div>
      <div class="modal-footer">
        <a type="button" class="btn btn-outline-primary" href="/api/payments">Kembali</a>
      </div>
    </div>
  </div>
</div>
<div class="modal-backdrop fade show"></div>

</body>
<script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
</html>
