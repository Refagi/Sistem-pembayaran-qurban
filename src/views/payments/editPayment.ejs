<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/bootstrap/css/bootstrap.min.css">
<style>
  .custom-width {
    max-width: 500px;
    background-color: #f2f2f2;
    border-radius: 10px;
  }
  .btn-primary {
    background-color: #263134;
    border-color: #263134;
  }
  /* .nav-link {
    color: #263134;
    transition: all 0.2s ease-in-out;
  }
  .nav-link:hover {
    color: #ffff !important;
    background-color: #2667ff;
  } */
</style>
</head>
<body>

<div class="container-lg p-5 ">
  <div class="row justify-content-center">
    <div class="col-10 col-md-8 col-lg-6 custom-width">

      <ul class="nav nav-pills nav-justified mb-3 mt-5 m-5" id="ex1" role="tablist">
        <li class="nav-item" role="presentation">
          <h3 class="text-center fw-bolder">Edit pembayaran <%= payment.name %></h3>
        </li>
      </ul>

      <div class="tab-content m-5">
        <div class="tab-pane fade show active" id="addAPayment" role="tabpanel" aria-labelledby="tab-addPayment">
          <% if (errorMessage) { %>
            <div class="alert alert-danger"><%= errorMessage %></div>
          <% } %>
          <form action="/api/payments/<%= payment.id %>?_method=PATCH" method="POST">

            <div class="form-outline mb-4 fw-bolder">
              <input type="text" id="edit-paymentStatus_animal" class="form-control" name="status_animal" value="<%= payment.status_animal %>" placeholder="Status Hewan" list="statusOptions" />
              <datalist id="statusOptions">
                <option value="available">
                <option value="sold">
              </datalist>
            </div>

            <div class="form-outline mb-4 fw-bolder">
              <input type="name" id="edit-paymentName" class="form-control" name="name" value="<%= payment.name %>" placeholder="Nama user" />
            </div>

            <div class="form-outline mb-4 fw-bolder">
              <input type="text" id="edit-paymentHandphone" class="form-control" name="handphone" value="<%= payment.handphone %>" placeholder="No handphone" />
            </div>

            <div class="form-outline mb-4 fw-bolder">
              <input type="text" id="edit-paymentInformation" class="form-control" name="information" value="<%= payment.information %>" placeholder="Informasi" />
            </div>

            <div class="text-center">
              <button type="submit" class="btn btn-outline-primary btn-block mb-4 mt-3">submit</button>
            </div>
          </form>
        </div>

      </div>
    </div>
  </div>
</div>

</body>
<script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
</html>
